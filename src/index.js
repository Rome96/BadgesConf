import React from 'react'
import ReactDOM from 'react-dom'

const element = <h1>Hola Universo</h1>
const container = document.getElementById('app')

// ReactDOM.render(__qué__, __dónde__);
ReactDOM.render(element, container)